<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        .layer {
            width: 1000px;
            margin: 0 auto;
        }
        .table {
            width: 1000px;
            text-align: center;
            border: 0.5px solid;
        }
        table td {
            border: 1px solid;
        }
    </style>
</head>
<body>
    <div class="layer">
        <table class="table">
            <caption>LOL Skill Cooldowns</caption>
            <thead>
                <tr>
                    <th>챔피언이름</th>
                    <th>소환사명</th>
                    <th>스킬쿨타임</th>
                </tr>
            </thead>
            <tbody>
                <% for(let i = 0; i < currentMatch.length; i++) { %>
                    <tr>
                        <td>
                            <img src="<%= `http://ddragon.leagueoflegends.com/cdn/${recentVersion}/img/champion/${currentMatch[i].championName}.png` %>" alt="">
                            <div><%= currentMatch[i].championName %></div>
                        </td>
                        <td><%= currentMatch[i].summonerName %></td>
                        <td>
                            <% for(let j = 0; j < 4; j++) { %>
                                <div style="display:inline-block;">
                                    <img src="<%= `http://ddragon.leagueoflegends.com/cdn/${recentVersion}/img/spell/${currentMatch[i].skillNames[j]}.png` %>" alt="">
                                    <div><%= JSON.stringify(currentMatch[i].skillCoolDowns[j]) %></div>
                                </div>
                            <% } %>
                        </td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </div>
</body>
</html>